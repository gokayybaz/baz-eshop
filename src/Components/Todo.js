

// ? Yapılacaklar

// ? React Router DOM eklenecek. [Tamamlandı]
// ? Pages klasörü oluşturulacak sayfalar ayrılacak ve component'ler de sayfa sayfa ayrılacak.
// ? Diğer sayfaların design işlemlerine başlanacak.
// ? Mobil ve responsive tasarıma geçilecek.
// ? State management eklenecek.

// ? Admin bölümü eklenecek.

// ? API Servis yapısı ile;
// ? * Authentication ve Authorization.
// ? * Ürün işlemleri.
// ? * Müşteri işlemleri.
// ? * Sepet işlemleri.
// ? * Kargo işlemleri.
// ? Yapılarak sistem canlıya hazır hale getirilecek.

// ? Test işlemleri için kodlar yazılacak.
// ? Performans optimizasyonları için gerekli işlemler yapılacak.

// ? Proje Canlıya alınacak ve sağlam bir readme dosyası hazırlanıp Linked'in üzerinden paylaşımı gerçekleştirilecek.
