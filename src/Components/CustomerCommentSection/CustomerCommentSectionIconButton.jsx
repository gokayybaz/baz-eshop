import React from 'react'

function CustomerCommentSectionIconButton({ icon }) {
    return (
        <button className='p-3 cursor-pointer border border-white hover:border-black rounded-full'>{icon}</button>

    )
}

export default CustomerCommentSectionIconButton